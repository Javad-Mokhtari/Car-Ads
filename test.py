import requests
from bs4 import BeautifulSoup
import html5lib
import re

values = data_dic['brand'], data_dic['model'], data_dic['year'], data_dic['worked'], data_dic['price'], data_dic['color'], data_dic['engine_status'], data_dic['chassis_status'], data_dic['body_status'], data_dic['insurance_deadline']
